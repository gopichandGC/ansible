- name: loops
  hosts: localhost
  become: yes
  tasks:
  - name: printing the installed packages
    ansible.builtin.package:
      name: "{{item}}"   # Item is a default keyword
      state: present 
    loop:
    - git
    - mysql
    - postfix
    - mongodb